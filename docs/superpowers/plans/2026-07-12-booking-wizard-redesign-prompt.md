# Prompt for Claude Code: Redesign the Booking Wizard (`/book`)

Paste everything below this line to Claude Code in the project root.

---

The booking wizard at `/book` needs a major visual and structural redesign. I've attached three screenshots of the current state (step 1 "Choose your experience", step 2 "Choose your vacation lesson", step 3 "Almost there — your details"). It currently reads like a generic SaaS checkout flow — a dark green rounded card floating on a sage background, boxed numbered tabs, two-tone banner buttons, and plain white pricing tiles. None of that matches the rest of the site's premium editorial identity. I want it redesigned to actually look like it belongs on this site.

Read `CLAUDE.md` first for full project context (stack, palette, type system, ghost-numeral motif, agent rules). Read it before doing anything else — it has rules you must not violate (protected About content, no invented pricing/copy, no placeholder text, etc. — none of that applies to this task, but read it anyway).

## Current implementation (read these before changing anything)

- `src/pages/Book.tsx` — the wizard component: step state machine (`type → participants → datetime → contact → confirm`), GSAP entrance/transition choreography, all form logic, hidden Formspree-ready fields, accessibility (focus management on step change, `aria-live` context bar, reduced-motion branches).
- `src/pages/Book.css` — all current styling, prefixed `.bw-*` and `.bwc-*` (calendar).
- `src/components/booking/BookingCalendar.tsx` — the custom calendar grid (month paging, day selection), styled by `.bwc-*` classes in `Book.css`.
- `src/layout/SiteLayout.tsx` — currently gives `/book` a special locked-viewport treatment: `site-shell--booking` forces `height: 100svh; overflow: hidden`, and Book is the only content route with **no** `SiteFooter` (see the `isHome || isBooking ? null : <SiteFooter />` line). This is the root cause of the "boxed-in form" feeling — every other content route (`/about`, `/faq`, etc.) scrolls normally in the page flow.
- `src/index.css` — shared design tokens and primitives, notably: `--maui-gold` / `--maui-gold-light` / `--maui-gold-soft` / `--maui-gold-faint` (the site's one accent, reserved for high-intent moments), `--home-sage-ink` (#17352a, dark forest green ink), `--label-size` / `--label-tracking` (the uppercase micro-label scale used everywhere), `--grain-url` (shared paper-grain texture), and the `.cp-*` primitives (`cp-section-label`, `cp-button`, `cp-form-row/field/label/control` — these last four are currently used *only* by Book.tsx, so they're free to change or drop). Also see `.route-shell`, `.route-shell--cinematic`, `.page-main`, `.page-main--cinematic` for how other routes lay out content, and `.site-shell--booking` (lines ~67–98, plus a mobile override ~590–602) for the rules to remove.
- `src/styles/mauiPalette.ts` — single source of truth for the five-stop Maui gradient (`MAUI_PALETTE_CSS_VARS`), injected inline on the `.bw` root. It's currently imported but its stops (`--maui-shadow/emerald/jungle/bay/amber(-soft/-faint)`) aren't actually referenced anywhere in `Book.css` — either use them purposefully in the new design or remove the now-pointless inline injection; don't leave it wired up unused.
- For the ghost-numeral / watermark motif reference, look at `src/components/about/AaronStorySections.css` (`.aaron-story__watermark`, ~line 134) and `src/components/home/StackedServicesDeck.tsx`/`.css` for the existing pattern (oversized low-alpha Fraunces italic numerals/words) — the booking wizard's redesign should reuse this idiom rather than invent a new one.

## Direction (confirmed, don't re-litigate)

1. **Break out of the locked-viewport "card in a box."** Make `/book` a normal scrolling content page like `/faq` or `/about`, not a fixed-height widget with its own internal scroll container. Remove the `site-shell--booking` height-lock and overflow rules from `index.css`, and in `SiteLayout.tsx` stop special-casing Book's shell/main classes — give it the same treatment as other non-home, non-cinematic routes. **Also give `/book` the standard `SiteFooter`** (currently it's the only content route without one — that's an existing inconsistency with the "Footer standardized on all routes" rule in `CLAUDE.md`, fix it as part of this pass).
2. **Full-bleed editorial, not a boxed panel.** Drop the dark-green rounded "canvas" card entirely. Content should sit directly on the page (cream background, dark ink type), with large Fraunces/Cormorant typography, generous whitespace, and the ghost-numeral motif used boldly (e.g., a large low-alpha numeral watermark behind each step, echoing the About chapter watermarks) instead of the current tiny numerals on boxed tabs.
3. **Restructure the step indicator.** Replace the four boxed "1 LESSON TYPE / 2 DATE & TIME / ..." tabs (with top borders and tiny corner ghost numerals) with something thinner and more editorial — e.g. a slim label row with a single progress rule/underline that fills toward the gold accent, or numerals treated as the ghost-watermark itself rather than a UI chrome element. Gold stays reserved for the active/high-intent state per the site's existing rule (gold = high-intent moments only).
4. **Restyle the option cards.** The two-tone banner buttons (step 1) and plain white pricing tiles (step 2) read as generic dashboard/SaaS components. Redesign these as something that matches the site's actual visual language — e.g. full-width editorial rows separated by thin rules, large italic Fraunces titles, a gold arrow accent on hover/selection (the site already uses a gold CTA-arrow accent elsewhere — reuse that idiom rather than colored card headers). Whatever you land on, it must not look like a Bootstrap/Material card.
5. **Restyle the date/time step and the review/contact step to match** — no more boxed "warm surface" card floating on the dark canvas (that card face won't exist anymore since the canvas is gone). Calendar and time slots should sit in the page's normal editorial flow. The review summary and the contact form should read as calm, minimal, underline-style fields rather than boxed inputs, consistent with a premium single-page product site rather than a checkout widget.

## Hard constraints — do not violate

- **Do not change the booking logic, state machine, step order, or data shape.** `BookingData`, `VACATION_LESSON_OPTIONS`, `ONGOING_LESSON_OPTIONS`, `TIME_SLOTS`, pricing, `getLessonPrice`, `formatBookingContext`, the datetime auto-advance behavior, `goTo`/`goBack`/`handleSubmit` — all business logic stays as-is. This is a visual/structural (markup + CSS) redesign, not a rewrite of the wizard's behavior.
- **Preserve the hidden Formspree-ready fields exactly as named** (`lessonType`, `participants`, `duration`, `price`, `date`, `timeSlot`) — `handleSubmit` still goes straight to the confirm step; submission itself stays unwired per `CLAUDE.md` (owner needs to supply a Formspree form ID first — don't invent one).
- **Do not invent pricing, copy, or business details.** Reuse the existing lesson descriptions/copy verbatim unless you're told otherwise.
- **Keep accessibility intact or improve it**: one `<h1>` for the page, step headings still get focus on step change (`headingRef.current?.focus()`), the `aria-live` context bar, `aria-current="step"` on the active progress item, keyboard-operable calendar/time-slot buttons, visible focus states, and a full `prefers-reduced-motion` fallback (static layout, no scroll-linked or entrance tweens) — test both motion and reduced-motion.
- **Keep the site's existing visual system**: Fraunces + Cormorant Garamond display over the sans body, cream/forest-green/sage palette, gold reserved for high-intent moments only, the existing ghost-numeral frequency (don't overdo it), and the single "Book a Lesson" language already in use — don't add new CTA copy variants.
- Add new dependencies only if clearly necessary (it shouldn't be).

## Verification

After implementing: run `npm run build`, `npm run lint`, and `npm run typecheck` — all three must pass. Manually check (or describe how you checked): the reduced-motion fallback, mobile layout at the site's `760px` breakpoint, keyboard navigation through all five steps, and that no bracketed/placeholder text was introduced. Update `docs/superpowers/plans/2026-07-12-audit-verification-log.md` or `CLAUDE.md`'s "Not implemented yet" section only if this changes what's true there — otherwise leave those files alone.
